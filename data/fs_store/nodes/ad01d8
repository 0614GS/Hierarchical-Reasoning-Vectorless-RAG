{"node_id": "ad01d8", "title": "2. Long-term (persistent) filesystem", "path": "long-term-memory > Long-term memory > How it works > 2. Long-term (persistent) filesystem", "content": "* Stored in a LangGraph Store (via `StoreBackend`)\n* Persists across all threads and conversations\n* Survives agent restarts\n* Accessed through paths prefixed with `/memories/`: `/memories/preferences.txt`", "summary": "Describes the persistent filesystem in LangGraph Store, accessible via paths prefixed with '/memories/', surviving agent restarts and accessible across threads.", "keywords": ["LangGraph Store", "StoreBackend", "persistent filesystem", "agent restarts", "threads", "conversations", "paths", "memories"]}