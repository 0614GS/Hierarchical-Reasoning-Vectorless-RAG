{"node_id": "a99bd8", "title": "Match decision order to actions", "path": "human-in-the-loop > Human-in-the-loop > Best practices > Match decision order to actions", "content": "The decisions list must match the order of `action_requests`:\n\n```python  theme={null}\nif result.get(\"__interrupt__\"):\n    interrupts = result[\"__interrupt__\"][0].value\n    action_requests = interrupts[\"action_requests\"]\n\n    # Create one decision per action, in order\n    decisions = []\n    for action in action_requests:\n        decision = get_user_decision(action)  # Your logic\n        decisions.append(decision)\n\n    result = agent.invoke(\n        Command(resume={\"decisions\": decisions}),\n        config=config\n    )\n```", "summary": "Ensure decisions align with the order of action requests in a human-in-the-loop system.", "keywords": ["human-in-the-loop", "action_requests", "decision order", "get_user_decision", "Command", "resume"]}