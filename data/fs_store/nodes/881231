{"node_id": "881231", "title": "1. Short-term (transient) filesystem", "path": "long-term-memory > Long-term memory > How it works > 1. Short-term (transient) filesystem", "content": "* Stored in the agent's state (via `StateBackend`)\n* Persists only within a single thread\n* Files are lost when the thread ends\n* Accessed through standard paths: `/notes.txt`, `/workspace/draft.md`", "summary": "Describes the short-term filesystem stored in the agent's state, which persists only within a single thread and is lost when the thread ends.", "keywords": ["StateBackend", "short-term filesystem", "single thread", "transient", "agent's state", "standard paths"]}