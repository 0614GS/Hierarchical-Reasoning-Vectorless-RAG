{"node_id": "99a98b", "title": "User preferences", "path": "long-term-memory > Long-term memory > Use cases > User preferences", "content": "Store user preferences that persist across sessions:\n\n```python  theme={null}\nagent = create_deep_agent(\n    store=InMemoryStore(),\n    backend=lambda rt: CompositeBackend(\n        default=StateBackend(rt),\n        routes={\"/memories/\": StoreBackend(rt)}\n    ),\n    system_prompt=\"\"\"When users tell you their preferences, save them to\n    /memories/user_preferences.txt so you remember them in future conversations.\"\"\"\n)\n```", "summary": "Describes how to store user preferences persistently across sessions using specific backend configurations.", "keywords": ["user preferences", "InMemoryStore", "CompositeBackend", "StateBackend", "StoreBackend", "memories"]}