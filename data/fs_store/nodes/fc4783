{"node_id": "fc4783", "title": "Use the same thread ID", "path": "human-in-the-loop > Human-in-the-loop > Best practices > Use the same thread ID", "content": "When resuming, you must use the same config with the same `thread_id`:\n\n```python  theme={null}\n# First call\nconfig = {\"configurable\": {\"thread_id\": \"my-thread\"}}\nresult = agent.invoke(input, config=config)\n\n# Resume (use same config)\nresult = agent.invoke(Command(resume={...}), config=config)\n```", "summary": "Describes the necessity of using the same `thread_id` in configuration when resuming an agent's operation.", "keywords": ["thread_id", "resume", "agent.invoke", "config", "configuration"]}