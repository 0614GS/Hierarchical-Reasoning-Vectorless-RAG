{"node_id": "c2e1d1", "title": "InMemoryStore (development)", "path": "long-term-memory > Long-term memory > Store implementations > InMemoryStore (development)", "content": "Good for testing and development, but data is lost on restart:\n\n```python  theme={null}\nfrom langgraph.store.memory import InMemoryStore\n\nstore = InMemoryStore()\nagent = create_deep_agent(\n    store=store,\n    backend=lambda rt: CompositeBackend(\n        default=StateBackend(rt),\n        routes={\"/memories/\": StoreBackend(rt)}\n    )\n)\n```", "summary": "Describes the InMemoryStore for testing and development purposes, noting data loss on restart.", "keywords": ["InMemoryStore", "testing", "development", "data loss", "restart", "CompositeBackend", "StateBackend", "StoreBackend"]}